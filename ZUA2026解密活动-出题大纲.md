# ZUA2026 新生网页解密活动 · 出题大纲与思路

> 郑州航空工业管理学院 计算机学院 · 2026 新生网页解密活动
> 适用对象：计算机学院大一新生（其他学院可自愿参加）
> 活动时长：约 1 周
> 本文档用途：出题总纲、思路说明、落地清单（供组织者迭代使用）

---

## 0. 一句话定位

给大一新生的"**第一场计算机冒险**"——用一周时间，通过一串网页谜题，
让新生第一次打开开发者工具、第一次解码、第一次用算法思维解决问题，
最后亲手解开"黑匣子"里的终极秘密。

**参照往年**：题目 + 输入框 → 太简陋。
**今年升级为**：故事线 + 关卡地图 + 交互式谜题 + 徽章收集 + 校园联动。

---

## 1. 活动概述

| 项目 | 设计 |
|---|---|
| 主题 | 航空 × 计算机（黑匣子 / 信号 / 算法） |
| 形式 | 网页闯关，答案驱动解锁（纯线上，线下彩蛋可选） |
| 时长 | 发布后一周内完成（约 13 关主干 + 每日彩蛋） |
| 通关标准 | 解出终极谜题，按完成时间排名 |
| 组织方 | 计算机学院（编程队 / ACM 队 / 编程社团协办） |
| 平台 | 自有 2C2G 服务器 + 服务端校验（防剧透/防作弊，见 §11） |

---

## 2. 核心创新点（对比往年的"题目 + 输入框"）

1. **故事线驱动**：全活动包裹在一个"航班 ZUA-2026 黑匣子失联"的叙事里，
   每关通关获得一块"黑匣子碎片"，剧情随关卡推进。题目不再是一堆干巴巴的列表。
2. **关卡地图与进度系统**：主页是一张"航线图"，点亮已通关卡；集齐碎片可视化。
3. **交互式关卡**：二分猜数、排序小游戏、汉诺塔、迷宫——谜题本身就是小游戏，
   而不是"读题 → 打字 → 提交"。
4. **网页本身即谜题**：线索藏在源代码注释、浏览器控制台、404 页面、
   `robots.txt` 里——让新生第一次真正"玩"网页，顺手学会 F12。
5. **游戏化运营**：积分、三级提示系统、徽章称号、排行榜、每日彩蛋、倒计时。
6. **校园彩蛋**：线下二维码 / 校园地标联动（可选加分项，见 §9）。

---

## 3. 总体玩法与流程

```
发布页（故事开场 + 新手教程）
        │
   关卡 1 → 2 → 3 → …（答案正确解锁下一关，每关掉落 1 块碎片）
        │
   每日彩蛋（与主干并行，每天 1 道趣味小题，不设门槛）
        │
   终极谜题（需要全部碎片拼出的密钥）
        │
   通关页（排名登记 + 隐藏成就 + 线下颁奖预告）
```

**关键规则**：
- 答案统一为**英文小写 + 数字**，校验时忽略大小写、空格、全半角（防挫败感）。
- 每关提供 3 级提示：第 1 级免费，之后逐级扣分（提示本身也是乐趣的一部分）。
- 积分 = 关卡基础分 + 时间加成 + 少用提示加成。
- 排行榜按"首次完成总时长"排序（可选，只展示昵称/学号后四位，注意隐私）。

---

## 4. 故事主线（示例，可替换）

> **2026 年 X 月，一架编号 ZUA-2026 的校园测绘无人机在执行任务时失联，
> 只传回一段加密信号。你是计算机学院"信号分析小组"的新成员，
> 需要逐关还原黑匣子数据，找回无人机，并揭开它失踪前拍到的画面……**

> 彩蛋：无人机主线天然呼应学院"低空技术"专业——终局可让无人机"拍到的画面"
> 指向低空技术/无人机相关的社团或实验室（可选联动）。

- 每关通关后播一段"剧情卡"推进故事（20~50 字即可）。
- 碎片设定：黑匣子被切成 13 片，每片是一个字符，拼起来是终极密钥。
- 结局彩蛋：找回无人机后，它拍到的"画面"是计算机学院 / 社团的一句暗号
  （把社团文化写进结局，是天然的收尾彩蛋）。

---

## 5. 难度曲线（循序渐进，开局不劝退）

```
难度 5 │                                        ◆ 终极
      │                                  ◆ ◆
难度 4 │                            ◆ ◆
      │                      ◆ ◆
难度 3 │                ◆ ◆
      │          ◆ ◆
难度 2 │    ◆ ◆ ◆ ◆
难度 1 │◆ ◆
      └──────────────────────────────────────────→ 关卡
      阶段一     阶段二      阶段三       阶段四
      启程       探秘        算法初体验    终局
      (1-4)     (5-8)      (9-12)       (13+彩蛋)
```

**原则**：
- 前 4 关**保证人人能过**：自带教程 + 免费提示 + 明显到"送你过"的线索。
- 中段开始要求"动动手"（F12、下载图片、记事本打开）。
- 后段是算法思维（交互式，不要求写代码）。
- 终极题只属于坚持到最后的少数人，制造成就感。

---

## 6. 关卡总览表

| 关 | 阶段 | 关卡名 | 知识点（浅） | 形式 | 学校元素 | 难度 |
|---|---|---|---|---|---|---|
| 1 | 启程 | 信号接收（教学关） | 查看网页源代码 / HTML 注释 | 看源码找注释 | 校训 / 学院名 | ★ |
| 2 | 启程 | 电码电报 | 摩斯码 | 文本解码 | "ZUA" 字样 | ★ |
| 3 | 启程 | 0 与 1 的世界 | 二进制 → ASCII | 文本解码 | 解码出学院/专业名 | ★ |
| 4 | 启程 | 编码不等于加密 | Base64 | 文本解码 | 混合航空词汇 | ★☆ |
| 5 | 探秘 | 浏览器里的侦探 | 开发者工具 / Console | 页面 + F12 | — | ★★ |
| 6 | 探秘 | 藏在图片里 | 文件元数据 / 简单隐写 | 下载图片分析 | 校园/航模照片 | ★★ |
| 7 | 探秘 | 404 未找到 | 404 页 / robots.txt | 故意触发 404 | 彩蛋文案 | ★★ |
| 8 | 探秘 | 真假难辨 | 布尔逻辑 / 逻辑题 | 逻辑推理 | — | ★★☆ |
| 9 | 算法 | 猜数大师 | 二分查找 O(log n) | **交互小游戏** | 上限 2026 | ★★ |
| 10 | 算法 | 排序实验室 | 冒泡/选择排序思想 | **交互小游戏** | — | ★★★ |
| 11 | 算法 | 汉诺塔的诅咒 | 递归思想 2ⁿ−1 | **交互小游戏** | — | ★★★ |
| 12 | 算法 | 斐波那契的尾巴 | 递推 / 循环节 | 文本 + 可选代码 | 第 2026 项 | ★★★☆ |
| 13 | 终局 | 黑匣子终章 | 多步组合密码 | 汇总碎片 + 转换 | 校史 / 社团暗号 | ★★★★ |
| 彩 | 终局 | 通关成就 + 隐藏彩蛋 | — | 彩蛋页 / 线下扫码 | 地标联动 | ★ |

> 每日彩蛋 7 道（与主干并行）：谐音梗、ASCII 艺术、键盘移位、表情包解码等，见 §12 备选题库。

---

## 7. 分阶段详细关卡设计

### 阶段一 · 启程（第 1–4 关）—— 教工具，送过关

> 定位：让 100% 参与者拿到第一块碎片，建立信心；顺带教会"解码三件套"。

**第 1 关「信号接收」**（教学关，★）
- 页面上一段剧情，正文写："重要信息从不摆在明面上。试着**查看网页源代码**，
  线索就藏在注释里。右键 → 查看页面源代码（手机上可用 `view-source:` 链接）。"
- 源码注释里放第一关答案与下一关提示。
- 答案示例：`zua`（或校训拼音，**用校训更好，见待核实清单**）。
- 送分设计：页面上直接写"答案是一个三个字母的缩写"。

**第 2 关「电码电报」**（摩斯码，★）
- 给一段摩斯码文本（. 与 -），剧情说"这是失联前最后一段电报"。
- 页面提供解码工具链接（或内置一个输入提示）。
- 答案示例：`welcome`。

**第 3 关「0 与 1 的世界」**（二进制 → ASCII，★）
- 给一段 8 位二进制串，解码出单词。
- 剧情彩蛋：解码结果就是本关答案，比如 `cs`（计算机科学）或学院英文名。
- 答案示例：`computer`。

**第 4 关「编码不等于加密」**（Base64，★☆）
- 给一段 Base64，解码后是一句提示："编码只是换了表达方式，不是加密。"
- 答案示例：`aviation`（顺便科普航空词汇）。

> 阶段一小结卡："恭喜，你已学会查看源码、摩斯码、二进制、Base64——
> 这是所有解谜的基本功。" 解锁徽章「信号实习生」。

### 阶段二 · 探秘（第 5–8 关）—— 动手动脑

> 定位：把"网页"本身变成谜题，让新生第一次用上 F12。

**第 5 关「浏览器里的侦探」**（F12 / Console，★★）
- 页面正常内容什么都没有，但 `console.log` 里输出一段话（含答案线索）。
- 提示：按 F12 打开开发者工具 → Console 面板。
- 进阶分支：Network 面板里某个请求的响应头/JSON 里藏一个字段（对新生略难，可做加分题）。
- 适配：此关**建议电脑端**，页面顶部标注；手机端提供 `view-source:` 链接替代。

**第 6 关「藏在图片里」**（元数据 / 简单隐写，★★）
- 给一张校园照片（图书馆/航模/机房）。
- 方案 A（简单）：文件属性"详细信息"里作者/备注字段藏答案。
- 方案 B（进阶）：用记事本打开图片，尾部追加文本（纯文本隐写，概念科普级别）。
- 方案 C（可选）：隐藏字幕图片（轻微色差叠加的文字，调对比度可见）。
- 答案示例：`library`（图书馆）。

**第 7 关「404 未找到」**（404 / robots.txt，★★）
- 正文写："这个页面好像丢了……你确定吗？"提供一个故意写成 404 的链接。
- 404 页本身藏线索；同时科普 `robots.txt`，网址尾部加 `/robots.txt` 有第二段线索。
- 答案示例：`404` 或特定单词。

**第 8 关「真假难辨」**（布尔逻辑，★★☆）
- 文字版逻辑题："三个人中只有一个人说真话……谁偷了黑匣子芯片？"
  或真值表填空题（A 且 B、A 或 B、非 A 的语义判断）。
- 答案示例：`and` 或人物代号。
- 剧情：黑匣子数据被"逻辑锁"加密，需要推理出真话者。

> 阶段二小结卡：解锁徽章「代码侦探」。

### 阶段三 · 算法初体验（第 9–12 关）—— ACM 思维，但不写代码

> 定位：活动最大亮点。用**交互小游戏**让新生"玩"出算法思想，
> 每关结束给一张"原来如此"科普卡，把体验翻译成术语。

**第 9 关「猜数大师」**（二分查找，★★）
- 网页实现猜数游戏：程序想一个 1~2026 的数，玩家猜，程序回答"大了/小了"。
- 硬性约束：**最多 11 次**（2¹¹=2048 > 2026），逼出"折半"策略。
- 通关后科普卡："你刚刚使用了**二分查找**，复杂度 O(log n)——ACM 第一课。"
- 彩蛋：猜数界面用航空塔台配色/雷达动画。

**第 10 关「排序实验室」**（排序思想，★★★）
- 页面给出 6 根乱序的"信号强度柱"，玩家每次只能**交换相邻两根**（冒泡）
  或**选择任意两根交换**（选择），在步数限制内排好。
- 通关后科普卡："这就是冒泡排序/选择排序。"
- 也可做变体：交换次数最少是多少？（引导逆序对概念，加分题）。

**第 11 关「汉诺塔的诅咒」**（递归思想，★★★）
- 内置 4 层汉诺塔小游戏（点击移动），要求用最少步数（2⁴−1=15）完成。
- 也可做成纯算术：给出 3 层/4 层塔的最少步数，问 5 层是多少？（2ⁿ−1）。
- 通关后科普卡："递归：把大问题拆成同样的小问题。"

**第 12 关「斐波那契的尾巴」**（递推 / 循环节，★★★☆）
- 剧情："黑匣子加密序列符合斐波那契规律，请找出第 2026 项的**个位数字**。"
- 解法 A（找规律）：个位数字每 60 项循环（皮萨诺周期，不用讲名词，只引导"找循环"）。
- 解法 B（可选加分）：页面给一段可复制的 Python 三行代码，
  提供在线运行链接（菜鸟工具等），跑出答案——**顺便让新生第一次"运行程序"**。
- 答案：`9`（斐波那契模 10 的第 2026 项个位，**出题时用程序实测确认**）。

> 阶段三小结卡：解锁徽章「算法学徒」，文案强调"竞赛离你并不远"，
> 附 ACM 队 / 编程社团招新入口（如 `acm.zua.fan`，见 §9）。

### 阶段四 · 终局（第 13 关 + 彩蛋）

**第 13 关「黑匣子终章」**（组合密码，★★★★）
- 前面每关碎片是 1 个字符，13 片拼成一句 13 字符的密钥短语。
- 拿到密钥后按剧情提示做最终转换（如：密钥 + 建校年份做移位 / 按提示取字），
  得到终极答案，填入终极输入框。
- 设计要点：**终极答案与学校强绑定**，例如是校训、学院一句话、
  或"编程队/社团的暗号"——只有真正走完全程的人才知道。
- 通关页：庆祝动画 + 完成时间 + 排名登记表单（学号/昵称）+ 隐藏成就。

**隐藏成就 / 彩蛋**：
- 彩蛋一：通关后页面上出现"无人机照片"（一张藏着社团二维码的图）。
- 彩蛋二（可选线下）：校园内 2~3 处张贴小二维码（图书馆、机房、社团活动室），
  扫出"彩蛋题"，答对得额外积分与实体小奖品。

---

## 8. ACM / 竞赛知识融入原则

1. **不要求写代码**：核心目标是让新生"体验"算法思维，代码是可选项。
2. **玩 → 术语**：先玩交互游戏，再给科普卡讲名字（二分、排序、递归、递推）。
3. **少量真实竞赛味**：2026、步数限制、最优化问题——都是 ACM 题目的影子，
   但都被降维成小游戏。
4. **代码加分题**（可选）：每道算法关附"想挑战真正的编程？"板块，
   给 3 行 Python 及在线运行链接，答对加积分——这是把新生引向 OJ 的第一步。
5. **联动招新**：通关页 / 阶段三小结卡放 ACM 队与编程社团的介绍
   （据公开信息，郑州航院 ACM 队伍站点为 `acm.zua.fan`，请以实际为准），
   活动收官即招新季的自然入口。

---

## 9. 学校与学院元素融入方案

| 层次 | 融入点 | 用法示例 |
|---|---|---|
| 校名 | ZUA / 郑州航空工业管理学院 | 航班号 ZUA-2026；缩写作密钥素材 |
| 校史 | 建校年份等校史节点 | 二进制 / ASCII 转换素材；终极题密钥成分 |
| 校训 | 校训原文 | 第 1 关答案或终极题答案（**待核实原文**） |
| 学院 | 计算机学院：计算机科学与技术 / 软件工程 / 网络工程 / 低空技术（已核实） | 解码结果、答案词库；"低空技术"专业与无人机剧情天然契合（宣传噱头） |
| 编程队/社团 | ACM 队、编程社团 | 故事结局暗号；收官招新入口；线下活动联合 |
| 校园地标 | 中心广场（已核实）、图书馆、教学楼、机房、东校区等 | 图片隐写素材；线下二维码张贴点 |
| 航空特色 | 黑匣子、雷达、航班、航模、北斗 | 全剧情包装 + 部分答案词（aviation、radar 等） |

### ⚠️ 待核实清单（请组织者按官网/实况确认后填入）
- [x] 计算机学院完整专业列表：计算机科学与技术、软件工程、网络工程、低空技术 ✅
- [x] 广场名称：中心广场 ✅
- [ ] 校训原文及准确表述
- [ ] 建校年份与关键校史节点（如 X 年升本、X 年更名）
- [ ] 图书馆、机房等其余地标准确名称与楼栋编号
- [ ] ACM 队 / 编程社团的官方名称、场地、联系人、OJ/站点地址
- [ ] 学院公众号 / 宣传渠道账号名
- [ ] 活动是否需要院级备案 / 辅导员支持 / 奖品经费

---

## 10. 游戏化与运营机制

- **积分**：基础分 100/关；完成越快加分越多；少用提示加分；每日彩蛋 20 分/道。
- **徽章 / 称号**：信号实习生 → 代码侦探 → 算法学徒 → 黑匣子破译师。
- **三级提示**：每关 3 个提示按钮，第 1 个免费，之后逐级扣 10 分/20 分。
- **排行榜**：按首次通关总时长排名（可选，隐私注意：只显示昵称或学号后四位）。
- **每日彩蛋**：主干之外每天 1 道趣味小题（谐音梗、ASCII 艺术、键盘移位…），
  答对得积分；不设门槛，保证"每天都有事做"。
- **倒计时**：首页显示距截止的倒计时，制造一周紧迫感。
- **验收数据**：记录每关通过人数，找出"劝退关"（通过率骤降的关卡）复盘。

---

## 11. 技术实现建议（已定：自建 2C2G 服务器 + 服务端校验）

**结论**：答案与关卡内容只存在服务器，前端零机密。
纯前端方案已被否决——F12/Network 会把答案、校验逻辑、未解锁关卡全部暴露。

### 整体架构
```
浏览器（静态前端：展示 + 提交答案）
        │ HTTPS
Nginx / Caddy 反代（443 → 应用端口，Let's Encrypt 免费证书）
        │
Node.js (Express/Fastify) 或 Python (FastAPI)
        │
SQLite 单文件（players / solves 两张表）
```
- 前端只负责展示与提交，不含任何答案与未解锁关卡
- 数据库用 SQLite：几百人规模绰绰有余、零运维，**不要上 MySQL/Redis**
- 2C2G 完全够用：Node 常驻内存 <200MB，100 并发答题毫无压力
- 进程守护：PM2（或 Docker Compose）；安全组只开 80/443/SSH

### 数据模型（两张表就够）
- `players`：id / session_token / nickname / created_at
- `solves`：player_id + level_id + solved_at（联合唯一约束防重复提交）
- 答案不落库（写在服务端配置里）即可；落库则存 SHA-256 哈希（防服务器被拖库后泄露）

### API 设计
| 接口 | 作用 |
|---|---|
| `GET /api/levels` | 返回已解锁关卡列表（按 solves 计算） |
| `GET /api/levels/:id` | 返回该关题面 + 剧情卡（未解锁返回 403） |
| `POST /api/levels/:id/check` | 校验答案；正确则写 solves + 下发下一关解锁 |
| `POST /api/levels/:id/hints` | 扣分换提示 |
| `GET /api/rank` | 排行榜（按首次通关时间） |
| `POST /api/finish` | 终极题通过，记录完成时间 |

### 防作弊思路（服务端三件套）
1. **解锁令牌机制**：答对第 N 关才下发第 N+1 关内容 → 前端拿不到未解锁关卡
2. **限流 + 答错冷却**：同一会话答错后冷却 3~5 秒、每分钟限次数
   （express-rate-limit 级别即可）→ 防字典爆破
3. **榜单按首次通过时间**：社交作弊（截图分享答案）无法根除，但这个规则让
   作弊对排名无意义；发奖前用学号绑定核验身份

### 运维要点
- 参数化查询防注入；日志记录答错尝试（发现刷子）；每日 cron 备份 SQLite 文件（几 MB）
- 域名：没有的话找学院网络中心申请子域名，或用免费域名（如 `.eu.org`）；
  HTTPS 证书 certbot 自动续期；确认服务器校外可访问（校园网出口策略）
- 上线前压测：模拟 50~100 并发调 check 接口，确认无瓶颈

### 开发节奏（先最小闭环，再填充）
```
会话注册 → 第 1 关题面 → 校验答案 → 解锁第 2 关 → 排行榜 → 终极题 → 通关页
```
题库做成 JSON 配置（关卡即数据），后端读配置，换题不动代码。
内测（5~8 人试玩）用同一套服务，顺便验证并发与防爆破。

---

## 12. 备选题库（20+ 题，用于替换或每日彩蛋）

1. 键盘相邻键替换密码（如 "sword" → 左移一位）
2. ASCII 艺术图猜词（大字 `ZUA` 由字符拼成）
3. 谐音梗（"零一" → 01）
4. 表情符号替换密码（🛫=起飞=depart）
5. 拼音首字母缩写
6. T9 九宫格键盘数字转字母
7. 图片反色/负片后显示文字
8. 图片放大数倍后隐藏像素字
9. 文件属性（作者/备注/标题）藏答案
10. Unix 时间戳转日期
11. 十六进制颜色码 `#RRGGBB` 藏汉字编码信息
12. HTML 实体（`&#12290;` 等）解码
13. URL 编码（`%E8%88%AA` → 航）
14. 大小写隐藏信息（大写=1 小写=0 → 二进制）
15. 摩斯码变体（长短音/快慢节奏）
16. 数独/逻辑填字小格子
17. 一笔画判定（奇点数量——图论第一课）
18. 汉诺塔最少步数算术题
19. 斐波那契个位循环节找规律
20. 闰年判断（能被 4 整除且不被 100 整除，或能被 400 整除）
21. 进制综合转换（16 进制 → 2 进制 → 8 进制）
22. 逻辑门符号识别（与或非 → 真值 → 字符）
23. 二维码（页面上一个二维码，扫出文字）
24. 井字棋必胜策略推理
25. 过河/电梯经典逻辑题（人狼羊菜）

---

## 13. 宣传与时间规划

**时间线（建议）**：
```
第 1–2 周  定大纲、定故事线、分配出题、准备素材（照片/地标）
第 3 周    完成 80% 关卡 + 技术搭建 + 题库 JSON
第 4 周    内测（5–8 人试玩，校准难度与答案）
第 5 周    修题、上线、宣传物料（推文/海报/二维码）
第 6 周    发布 → 一周进行（每日彩蛋 + 每日推文/群播报）→ 收官颁奖 + 复盘
```

**宣传渠道**（已定：以线下 + 社团群为主，其他渠道待定）：
- 线下：教学楼、宿舍楼、机房、**中心广场**贴海报/二维码（扫码即玩）
- 社团群：编程队 / ACM 队 / 编程社团群内宣发（也作为招新预热）
- 班群 / 新生群转发（辅导员或班助协助，需确认）
- 学院公众号推文（待确认是否可行）
- 收官：群内/公众号公布榜单、通关名单、颁奖照片、活动复盘

**宣传语（已定：低空热点 × 新手向结合）**：
- 主标题：「**校园无人机失联了——计算机学院请你来破译它的黑匣子**」
- 副标题：「ZUA-2026 新生网页解密活动 · 零基础也能玩，一周带你入门计算机」
- 卖点句：「不用会编程，会好奇就能玩」+ 低空技术 × 计算机双专业联动
- 海报元素：无人机剪影 + 雷达波纹 + 二维码 + 小字彩蛋「第一关的答案就藏在网页里」

**奖品建议**（预算友好）：
- 通关奖：U 盘 / 键帽 / 徽章 / 零食包
- 前三名：机械键盘 / 图书（《算法图解》等）/ 院级证书（需与学院确认）
- 参与奖：社团纪念周边

---

## 14. 风险与对策

| 风险 | 对策 |
|---|---|
| 答案被剧透/被爬 | 接受，榜单按首次完成时间；文案声明"剧透失去的是自己的乐趣" |
| 手机端体验差 | 电脑关标注 + `view-source` 替代；主干关卡手机可用 |
| 难度失控（劝退关） | 内测数据校准；每关 3 级提示；备选题库随时替换 |
| 参与人数少 | 前 4 关送分式设计 + 宣传铺开 + 奖品刺激 |
| 服务器/域名成本 | 已有 2C2G 自建服务器；域名用学院子域名或免费域名；确认校外访问策略 |
| 出题者自己卡住 | 所有答案出题时用程序/工具实测一遍，双人复核 |
| 进度丢失 | localStorage 说明 + 通关后立刻登记表单 |
| 活动时间撞考试周 | 提前与学院确认档期 |

---

## 15. 下一步行动（组织者视角）

1. **定版本**：主干 13 关 + 7 每日彩蛋（可砍到 10+5）
2. **核实** §9 待核实清单（校训、校史、地标、社团信息）
3. **分配出题**：每人 2~3 关，按本文档的关卡规格表出题
4. **搭骨架**：先做关卡地图 + 题库 JSON + 校验逻辑，再填充关卡
5. **内测**：真实数据校准后发布

---

*参考信息：郑州航空工业管理学院官网 [zua.edu.cn](https://www.zua.edu.cn/)；
郑州航院 ACM 相关站点 [acm.zua.fan](https://acm.zua.fan/)（以实际官方信息为准）。
文中标注"示例"的校史/地标/答案均为占位，正式出题前须核实。*
